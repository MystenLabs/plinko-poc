export type PoolObject = {
    objectId: string;
    digest: string;
    version: string;
    type: string;
    balance?: number;
};
/**
 * A map of pool objects, where the keys are object IDs and the values are pool objects.
 */
export type PoolObjectsMap = Map<string, PoolObject>;
